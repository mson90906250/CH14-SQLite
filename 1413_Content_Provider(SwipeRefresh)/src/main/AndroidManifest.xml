<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tw.tcnr01.m1405">
    <uses-permission android:name="tw.tcnr01.m1405.provider.WRITE"></uses-permission>
    <uses-permission android:name="tw.tcnr01.m1405.provider.READ"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".M1405">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:name=".providers.FriendsContentProvider"
            android:authorities="tw.tcnr01.m1405.providers.FriendsContentProvider">

        </provider>

    </application>

</manifest>